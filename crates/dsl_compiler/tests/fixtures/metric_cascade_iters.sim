// Metric block — cascade iterations per tick, with alert.
metric {
  metric cascade_iters_per_tick = histogram(cascade.iterations)
    window 1000
    emit_every 100
    alert when max_bin > 0.90

  metric alive_agents = gauge(count(a in agents where a.alive))
    emit_every 10
    alert when value < 1
}
