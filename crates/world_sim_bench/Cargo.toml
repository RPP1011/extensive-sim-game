[package]
name = "world_sim_bench"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
game = { path = "../.." }
bincode = "1.3"
anyhow = "1"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "movement"
harness = false
