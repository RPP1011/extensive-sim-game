// GENERATED by dsl_compiler from assets/sim/physics.sim.
// Edit the .sim source; rerun `cargo run --bin xtask -- compile-dsl`.
// Do not edit by hand.

use crate::event::{Event, EventRing};
use crate::ids::AgentId;
use crate::state::SimState;

#[allow(unused_variables)]
pub fn rally_on_wound(a: AgentId, t: AgentId, state: &mut SimState, events: &mut EventRing) {
    if state.agent_alive(t) {
        let cur_hp = state.agent_hp(t).unwrap_or(0.0);
        let max_hp = state.agent_max_hp(t).unwrap_or(0.0);
        if (max_hp > 0.0) {
            let hp_pct = (cur_hp / max_hp);
            if (hp_pct < 0.5) {
                for kin in crate::spatial::nearby_kin(state, t, 12.0) {
                    events.push(Event::RallyCall {
                        observer: kin,
                        wounded_kin: t,
                        tick: state.tick,
                    });
                }
            }
        }
    }
}
