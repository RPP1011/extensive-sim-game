// GENERATED by dsl_compiler from assets/sim/physics.sim.
// Edit the .sim source; rerun `cargo run --bin xtask -- compile-dsl`.
// Do not edit by hand.
//
// NOTE: `state.views` is DELETED (Plan B1' Task 11). This generated fn
// previously called `state.views.standing.adjust(...)`. The standing view
// moves to `engine_rules`; until Task 11 lands, this fn is a no-op stub so
// the engine crate compiles. Tests that exercise standing are `#[ignore]`d.

use crate::event::EventRing;
use engine_data::events::Event;
use crate::ids::AgentId;
use crate::state::SimState;

#[allow(unused_variables)]
pub fn modify_standing(
    _a: AgentId,
    _b: AgentId,
    _delta: i16,
    _state: &mut SimState,
    _events: &mut EventRing<Event>,
) {
    // Re-enable after B1' Task 11 emits engine_rules::step::step and the
    // ViewRegistry moves to engine_rules::state.
}
