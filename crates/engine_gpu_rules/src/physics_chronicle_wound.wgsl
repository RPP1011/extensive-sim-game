// GENERATED by dsl_compiler — do not edit by hand.
// Regenerate with `cargo run --bin xtask -- compile-dsl`.

struct PhysicsChronicleWoundCfg { agent_cap: u32, _pad0: u32, _pad1: u32, _pad2: u32 };

@group(0) @binding(0) var<storage, read> event_ring_0: array<u32>;
@group(0) @binding(1) var<storage, read_write> event_ring_37: array<atomic<u32>>;
@group(0) @binding(2) var<storage, read> agent_hp: array<f32>;
@group(0) @binding(3) var<storage, read> agent_max_hp: array<f32>;
@group(0) @binding(4) var<storage, read> agent_alive: array<u32>;
@group(0) @binding(5) var<uniform> cfg: PhysicsChronicleWoundCfg;

@compute @workgroup_size(64)
fn cs_physics_chronicle_wound(@builtin(global_invocation_id) gid: vec3<u32>) {
let event_idx = gid.x;
// Indirect dispatch on event_ring_0; tail count bounds gid.x.

// op#20 (physics_rule)
let local_2: u32 = event_ring[event_idx * 10u + 2u];
let local_0: u32 = event_ring[event_idx * 10u + 3u];
if (false) {
    let local_1: f32 = 0.0;
    let local_3: f32 = 0.0;
    if ((local_3 > 0.0)) {
        let local_4: f32 = (local_1 / local_3);
        if ((local_4 < 0.5)) {
            emit_event_37(field_0: 4u, field_1: local_2, field_2: local_0);
        }
    }
}
}
