// GENERATED by dsl_compiler — do not edit by hand.
// Regenerate with `cargo run --bin xtask -- compile-dsl`.

/// PingPong-lifetime ring buffers (cascade A/B).
pub struct PingPongContext {
    /// Cascade-physics A-ring event records (write side at iter 0).
    pub events_a_records: wgpu::Buffer,
    /// Cascade-physics A-ring tail (atomic counter).
    pub events_a_tail: wgpu::Buffer,
    /// Cascade-physics B-ring event records.
    pub events_b_records: wgpu::Buffer,
    /// Cascade-physics B-ring tail.
    pub events_b_tail: wgpu::Buffer,
}

impl PingPongContext {
    pub fn new(_device: &wgpu::Device) -> Self {
        Self {
            events_a_records: _device.create_buffer(&wgpu::BufferDescriptor {
                label: Some("engine_gpu_rules::pingpong::events_a_records"),
                size: 4,
                usage: wgpu::BufferUsages::STORAGE | wgpu::BufferUsages::COPY_DST | wgpu::BufferUsages::COPY_SRC,
                mapped_at_creation: false,
            }),
            events_a_tail: _device.create_buffer(&wgpu::BufferDescriptor {
                label: Some("engine_gpu_rules::pingpong::events_a_tail"),
                size: 4,
                usage: wgpu::BufferUsages::STORAGE | wgpu::BufferUsages::COPY_DST | wgpu::BufferUsages::COPY_SRC,
                mapped_at_creation: false,
            }),
            events_b_records: _device.create_buffer(&wgpu::BufferDescriptor {
                label: Some("engine_gpu_rules::pingpong::events_b_records"),
                size: 4,
                usage: wgpu::BufferUsages::STORAGE | wgpu::BufferUsages::COPY_DST | wgpu::BufferUsages::COPY_SRC,
                mapped_at_creation: false,
            }),
            events_b_tail: _device.create_buffer(&wgpu::BufferDescriptor {
                label: Some("engine_gpu_rules::pingpong::events_b_tail"),
                size: 4,
                usage: wgpu::BufferUsages::STORAGE | wgpu::BufferUsages::COPY_DST | wgpu::BufferUsages::COPY_SRC,
                mapped_at_creation: false,
            }),
        }
    }
}
