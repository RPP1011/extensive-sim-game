// GENERATED by dsl_compiler::emit_binding_sources. Do not edit by hand.
// Regenerate with `cargo run --bin xtask -- compile-dsl`.

use crate::resident_context::ResidentPathContext;
use crate::pingpong_context::PingPongContext;
use crate::pool::Pool;
use crate::transient_handles::TransientHandles;
use crate::external_buffers::ExternalBuffers;

/// Aggregate of every container kernels can pull buffers from.
/// `Kernel::bind` takes `&BindingSources<'a>` and walks into the
/// right field; each kernel's emitted bind() body is real
/// generated code, not `unimplemented!()`.
pub struct BindingSources<'a> {
    pub resident:  &'a ResidentPathContext,
    pub pingpong:  &'a PingPongContext,
    pub pool:      &'a Pool,
    pub transient: &'a TransientHandles<'a>,
    pub external:  &'a ExternalBuffers<'a>,
}
