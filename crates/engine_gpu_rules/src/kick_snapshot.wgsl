// GENERATED by dsl_compiler — do not edit by hand.
// Regenerate with `cargo run --bin xtask -- compile-dsl`.

struct KickSnapshotCfg { agent_cap: u32, _pad0: u32, _pad1: u32, _pad2: u32 };

@group(0) @binding(0) var<storage, read_write> snapshot_kick: array<atomic<u32>>;
@group(0) @binding(1) var<uniform> cfg: KickSnapshotCfg;

@compute @workgroup_size(1)
fn cs_kick_snapshot(@builtin(global_invocation_id) gid: vec3<u32>) {
if (gid.x != 0u) { return; }

// op#32 (plumbing)
// PlumbingKind::KickSnapshot — flag the snapshot_kick slot;
// host-side snapshot pipeline observes the flag post-tick.
atomicStore(&snapshot_kick[0], 1u);
}
