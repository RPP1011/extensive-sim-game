// Placeholder lib.rs — regenerated by dsl_compiler from `assets/sim/*.sim`.
//
// During Phase 7's wolf-sim wipe (2026-05-02), the previously-generated
// kernel module set was deleted because the DSL inputs they were emitted
// from no longer exist. The crate keeps this empty placeholder so the
// workspace continues to build; running `cargo run -p xtask --bin xtask --
// compile-dsl --cg-canonical` against a new DSL fixture regenerates this
// file with the appropriate `pub mod ...` / `pub use ...` declarations.
