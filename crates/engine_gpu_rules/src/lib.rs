// GENERATED by dsl_compiler::emit_kernel_index. Do not edit by hand.
// Regenerate with `cargo run --bin xtask -- compile-dsl`.

#![allow(clippy::all)]
#![allow(unused_imports)]

pub mod apply_actions;
pub mod fused_mask;
pub mod mask_unpack;
pub mod scoring;
pub mod scoring_unpack;

pub mod schedule;
pub mod resident_context;
pub mod pingpong_context;
pub mod pool;
pub mod transient_handles;
pub mod external_buffers;
pub mod binding_sources;
pub mod megakernel;

#[derive(Copy, Clone, Debug, Eq, PartialEq, Hash)]
pub enum KernelId {
    ApplyActions,
    FusedMask,
    MaskUnpack,
    Scoring,
    ScoringUnpack,
}

#[derive(Copy, Clone, Debug, Eq, PartialEq, Hash)]
pub enum BufferRef {
    /// Resident path indirect-args buffer.
    ResidentIndirectArgs,
    /// Cascade-physics event ring tail buffer (gates further iterations when zero).
    CascadeRingTail,
}

pub trait Kernel {
    type Bindings<'a> where Self: 'a;
    type Cfg: bytemuck::Pod + bytemuck::Zeroable + Copy;

    fn new(device: &wgpu::Device) -> Self where Self: Sized;
    fn build_cfg(&self, state: &engine::state::SimState) -> Self::Cfg;
    fn bind<'a>(&'a self, sources: &'a binding_sources::BindingSources<'a>, cfg: &'a wgpu::Buffer) -> Self::Bindings<'a>;
    fn record(&self, device: &wgpu::Device, encoder: &mut wgpu::CommandEncoder, bindings: &Self::Bindings<'_>, agent_cap: u32);
}
