// GENERATED by dsl_compiler — do not edit by hand.
// Regenerate with `cargo run --bin xtask -- compile-dsl`.

use crate::binding_sources::BindingSources;

pub struct SeedIndirect0Kernel {
    pipeline: wgpu::ComputePipeline,
    bgl: wgpu::BindGroupLayout,
}

pub struct SeedIndirect0Bindings<'a> {
    pub event_ring: &'a wgpu::Buffer,
    pub indirect_args_0: &'a wgpu::Buffer,
    pub cfg: &'a wgpu::Buffer,
}

#[repr(C)]
#[derive(Copy, Clone, bytemuck::Pod, bytemuck::Zeroable)]
pub struct SeedIndirect0Cfg { pub agent_cap: u32, pub _pad: [u32; 3] }

const SHADER_SRC: &str = include_str!("seed_indirect_0.wgsl");

impl crate::Kernel for SeedIndirect0Kernel {
    type Bindings<'a> = SeedIndirect0Bindings<'a>;
    type Cfg = SeedIndirect0Cfg;

    fn new(device: &wgpu::Device) -> Self {
        let shader = device.create_shader_module(wgpu::ShaderModuleDescriptor {
            label: Some("engine_gpu_rules::seed_indirect_0::wgsl"),
            source: wgpu::ShaderSource::Wgsl(SHADER_SRC.into()),
        });
        let bgl = device.create_bind_group_layout(&wgpu::BindGroupLayoutDescriptor {
            label: Some("engine_gpu_rules::seed_indirect_0::bgl"),
            entries: &[
                crate::bgl_storage(0, true), // event_ring
                crate::bgl_storage(1, false), // indirect_args_0
                crate::bgl_uniform(2), // cfg
            ],
        });
        let pl = device.create_pipeline_layout(&wgpu::PipelineLayoutDescriptor {
            label: Some("engine_gpu_rules::seed_indirect_0::pl"),
            bind_group_layouts: &[&bgl],
            push_constant_ranges: &[],
        });
        let pipeline = device.create_compute_pipeline(&wgpu::ComputePipelineDescriptor {
            label: Some("engine_gpu_rules::seed_indirect_0::pipeline"),
            layout: Some(&pl),
            module: &shader,
            entry_point: Some("cs_seed_indirect_0"),
            compilation_options: Default::default(),
            cache: None,
        });
        Self { pipeline, bgl }
    }

    fn build_cfg(&self, state: &engine::state::SimState) -> SeedIndirect0Cfg {
        let _ = state;
        SeedIndirect0Cfg { agent_cap: state.agent_cap(), _pad: [0; 3] }
    }

    fn bind<'a>(&'a self, sources: &'a BindingSources<'a>, cfg: &'a wgpu::Buffer) -> SeedIndirect0Bindings<'a> {
        let _ = sources;
        SeedIndirect0Bindings {
            event_ring: sources.transient.cascade_current_ring,
            indirect_args_0: sources.transient.cascade_indirect_args,
            cfg: cfg,
        }
    }

    fn record(&self, device: &wgpu::Device, encoder: &mut wgpu::CommandEncoder, bindings: &SeedIndirect0Bindings<'_>, _agent_cap: u32) {
        let bg = device.create_bind_group(&wgpu::BindGroupDescriptor {
            label: Some("engine_gpu_rules::seed_indirect_0::bg"),
            layout: &self.bgl,
            entries: &[
                wgpu::BindGroupEntry { binding: 0, resource: bindings.event_ring.as_entire_binding() },
                wgpu::BindGroupEntry { binding: 1, resource: bindings.indirect_args_0.as_entire_binding() },
                wgpu::BindGroupEntry { binding: 2, resource: bindings.cfg.as_entire_binding() },
            ],
        });
        let mut pass = encoder.begin_compute_pass(&wgpu::ComputePassDescriptor {
            label: Some("engine_gpu_rules::seed_indirect_0::pass"),
            timestamp_writes: None,
        });
        pass.set_pipeline(&self.pipeline);
        pass.set_bind_group(0, &bg, &[]);
        pass.dispatch_workgroups(1, 1, 1);
    }
}

pub fn seed_indirect_0_bgl_entries() -> Vec<wgpu::BindGroupLayoutEntry> {
    vec![
                crate::bgl_storage(0, true), // event_ring
                crate::bgl_storage(1, false), // indirect_args_0
                crate::bgl_uniform(2), // cfg
    ]
}

pub fn seed_indirect_0_bind_group_entries<'a>(
    bindings: &'a SeedIndirect0Bindings<'a>,
) -> Vec<wgpu::BindGroupEntry<'a>> {
    vec![
                wgpu::BindGroupEntry { binding: 0, resource: bindings.event_ring.as_entire_binding() },
                wgpu::BindGroupEntry { binding: 1, resource: bindings.indirect_args_0.as_entire_binding() },
                wgpu::BindGroupEntry { binding: 2, resource: bindings.cfg.as_entire_binding() },
    ]
}

pub fn seed_indirect_0_build_cfg(state: &engine::state::SimState) -> SeedIndirect0Cfg {
    SeedIndirect0Cfg { agent_cap: state.agent_cap(), _pad: [0; 3] }
}
