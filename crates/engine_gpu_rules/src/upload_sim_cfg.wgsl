// GENERATED by dsl_compiler — do not edit by hand.
// Regenerate with `cargo run --bin xtask -- compile-dsl`.

struct UploadSimCfgCfg { agent_cap: u32, _pad0: u32, _pad1: u32, _pad2: u32 };

@group(0) @binding(0) var<uniform> sim_cfg: SimCfg;
@group(0) @binding(1) var<uniform> cfg: UploadSimCfgCfg;

@compute @workgroup_size(1)
fn cs_upload_sim_cfg(@builtin(global_invocation_id) gid: vec3<u32>) {
if (gid.x != 0u) { return; }

// op#17 (plumbing)
// PlumbingKind::UploadSimCfg — host-side upload (queue.
// write_buffer). Body is a no-op; the spec declares sim_cfg as
// a Uniform binding which is read-only inside the shader.
}
