// GENERATED by dsl_compiler — do not edit by hand.
// Regenerate with `cargo run --bin xtask -- compile-dsl`.

struct FoldEngagedWithCfg { event_count: u32, tick: u32, _pad0: u32, _pad1: u32 };

@group(0) @binding(0) var<storage, read> event_ring: array<u32>;
@group(0) @binding(1) var<storage, read> event_tail: array<u32>;
@group(0) @binding(2) var<storage, read_write> view_storage_primary: array<u32>;
@group(0) @binding(3) var<storage, read_write> view_storage_anchor: array<u32>;
@group(0) @binding(4) var<storage, read_write> view_storage_ids: array<u32>;
@group(0) @binding(5) var<storage, read> sim_cfg: array<u32>;
@group(0) @binding(6) var<uniform> cfg: FoldEngagedWithCfg;

@compute @workgroup_size(64)
fn cs_fold_engaged_with(@builtin(global_invocation_id) gid: vec3<u32>) {
    let event_idx = gid.x;
    if (event_idx >= cfg.event_count) { return; }
    let tick = cfg.tick;

    // op#9 (view_fold)
    let local_2: u32 = event_ring[event_idx * 10u + 2u];
    let local_0: u32 = event_ring[event_idx * 10u + 3u];
    _ = (1u);

    // op#10 (view_fold)
    let local_2: u32 = event_ring[event_idx * 10u + 2u];
    let local_0: u32 = event_ring[event_idx * 10u + 3u];
    _ = (1u);
}
