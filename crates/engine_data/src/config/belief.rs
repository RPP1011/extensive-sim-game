// GENERATED by dsl_compiler from assets/sim/config.sim.
// Edit the .sim source; rerun `cargo run --bin xtask -- compile-dsl`.
// Do not edit by hand.

use serde::{Deserialize, Serialize};

/// Compiler-emitted tunables for the `belief` config block.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(default)]
pub struct BeliefConfig {
    pub observation_range: f32,
    pub loud_observation_range: f32,
    pub decay_rate: f32,
    pub eviction_threshold: f32,
}

impl Default for BeliefConfig {
    fn default() -> Self {
        Self {
            observation_range: 10.0_f32,
            loud_observation_range: 25.0_f32,
            decay_rate: 0.98_f32,
            eviction_threshold: 0.05_f32,
        }
    }
}
