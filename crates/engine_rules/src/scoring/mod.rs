// GENERATED by dsl_compiler. Do not edit by hand.
// Regenerate with `cargo run --bin xtask -- compile-dsl`.

// No `scoring` declarations in scope yet (milestone 4 fills
// this in). The engine's UtilityBackend reads the table via
// the re-export below; keeping the symbol stable lets the
// call site land before the first scoring row does.

/// Empty scoring table placeholder. Replaced by the emitter
/// when milestone 4 lands the first `scoring` declaration.
pub const SCORING_TABLE: &[()] = &[];
