// GENERATED by dsl_compiler from assets/sim/physics.sim.
// Edit the .sim source; rerun `cargo run --bin xtask -- compile-dsl`.
// Do not edit by hand.

use crate::event::EventRing;
use crate::ids::AgentId;
use crate::state::SimState;
use engine_data::events::Event;

#[allow(unused_variables)]
pub fn update_beliefs_on_death(dead: AgentId, state: &mut SimState, events: &mut EventRing<Event>) {
    let max_hp_val = state.agent_max_hp(dead).unwrap_or(0.0);
    let tick_val = state.tick;
    for observer in crate::spatial::nearby_kin(state, dead, state.config.belief.observation_range) {
        if state.agent_alive(observer) {
            #[cfg(feature = "theory-of-mind")]
            if let Some(__beliefs) = state.agent_cold_beliefs_mut(observer) {
                __beliefs.upsert(
                    dead,
                    engine_data::belief::BeliefState {
                        last_known_hp: 0.0,
                        last_known_max_hp: max_hp_val,
                        last_updated_tick: tick_val,
                        confidence: 1.0,
                        ..Default::default()
                    },
                );
            }
        }
    }
}
