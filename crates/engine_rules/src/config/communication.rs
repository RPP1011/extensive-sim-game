// GENERATED by dsl_compiler from assets/sim/config.sim.
// Edit the .sim source; rerun `cargo run --bin xtask -- compile-dsl`.
// Do not edit by hand.

use serde::{Deserialize, Serialize};

/// Compiler-emitted tunables for the `communication` config block.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(default)]
pub struct CommunicationConfig {
    pub max_announce_recipients: u32,
    pub max_announce_radius: f32,
    pub overhear_range: f32,
    pub default_vocal_strength: f32,
}

impl Default for CommunicationConfig {
    fn default() -> Self {
        Self {
            max_announce_recipients: 32u32,
            max_announce_radius: 80.0_f32,
            overhear_range: 30.0_f32,
            default_vocal_strength: 1.0_f32,
        }
    }
}
