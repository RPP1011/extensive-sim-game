// GENERATED by dsl_compiler from assets/sim/views.sim.
// Edit the .sim source; rerun `cargo run --bin xtask -- compile-dsl`.
// Do not edit by hand.

use engine::ids::AgentId;
use engine::state::SimState;

/// @lazy view — lowered from `view is_stunned(...)` in the sim DSL.
pub fn is_stunned(state: &SimState, a: AgentId) -> bool {
    (state.tick < state.agent_stun_expires_at(a).unwrap_or(0))
}
