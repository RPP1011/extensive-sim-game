// GENERATED by dsl_compiler from assets/sim/views.sim.
// Edit the .sim source; rerun `cargo run --bin xtask -- compile-dsl`.
// Do not edit by hand.

use crate::ids::AgentId;
use crate::state::SimState;

/// @lazy view — lowered from `view slow_factor(...)` in the sim DSL.
/// Pure expression body; re-evaluated on each call. Spec §2.3.
pub fn slow_factor(state: &SimState, a: AgentId) -> i16 {
    (if (state.tick < state.agent_slow_expires_at(a).unwrap_or(0)) {
        state.agent_slow_factor_q8(a).unwrap_or(0)
    } else {
        0
    })
}
