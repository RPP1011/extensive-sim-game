// Chase a target entity within an aggro radius.
// Imported via `import std/rules/chase.sim;` and applied via
// `physics MyChase = chase(target: SomeEntity, aggro: 10.0, speed: 1.0);`.
//
// NOTE: The body is intentionally empty. Body-substitution of param refs
// (aggro, speed, target) inside cloned handler bodies is deferred to T8 v2.
// This template tests the structural pass (parse → validate → monomorphise
// → emit two distinct concrete kernels) without requiring body-substitution.

physics chase(target: EntityKind, aggro: f32, speed: f32) @phase(per_agent) {
  on Tick {} {
  }
}
