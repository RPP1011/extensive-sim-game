# GENERATED by dsl_compiler from the `config` blocks in assets/sim/.
# Edit the .sim source; rerun `cargo run --bin xtask -- compile-dsl`.
# Hand edits to this file are appropriate for TUNING — changes will
# be read at runtime via Config::from_toml. Do NOT change the set of
# fields here; that bumps the config schema hash. Change values freely.

[combat]
attack_damage = 10.0
attack_range = 2.0
aggro_range = 50.0
engagement_range = 2.0
engagement_slow_factor = 0.3

[communication]
max_announce_recipients = 32
max_announce_radius = 80.0
overhear_range = 30.0
default_vocal_strength = 1.0
channel_speech_range = 30.0
channel_pack_range = 20.0
channel_pheromone_range = 40.0
channel_long_range_vocal = 200.0

[movement]
move_speed_mps = 1.0

[needs]
eat_restore = 0.25
drink_restore = 0.3
rest_restore = 0.15
