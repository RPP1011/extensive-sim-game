# GENERATED by dsl_compiler from the `config` blocks in assets/sim/.
# Was emitted by xtask `compile-dsl` (retired Phase 7, 2026-05-02);
# now hand-maintained for tuning. Hand edits to VALUES are
# appropriate — they're read at runtime via Config::from_toml. Do NOT
# change the set of FIELDS here; that bumps the config schema hash.

[belief]
observation_range = 10.0
loud_observation_range = 25.0
decay_rate = 0.98
eviction_threshold = 0.05

[combat]
attack_damage = 10.0
attack_range = 2.0
aggro_range = 50.0
engagement_range = 2.0
engagement_slow_factor = 0.3
global_cooldown_ticks = 5
kin_radius = 12.0
kin_flee_bias = 0.5
kin_flee_radius = 12.0

[communication]
max_announce_recipients = 32
max_announce_radius = 80.0
overhear_range = 30.0
default_vocal_strength = 1.0
channel_speech_range = 30.0
channel_pack_range = 20.0
channel_pheromone_range = 40.0
channel_long_range_vocal = 200.0

[movement]
move_speed_mps = 1.0
max_move_radius = 20.0

[needs]
eat_restore = 0.25
drink_restore = 0.3
rest_restore = 0.15
